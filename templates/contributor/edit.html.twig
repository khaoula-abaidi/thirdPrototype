{% extends 'base.html.twig' %}

{% block title %}Edit Contributor{% endblock %}

{% block body %}
    <h1>Edit Contributor</h1>

    {{ include('contributor/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('contributor_index') }}">back to list</a>

    {{ include('contributor/_delete_form.html.twig') }}
{% endblock %}
